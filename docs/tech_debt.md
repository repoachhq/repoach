# Tech-debt ledger

Known, deliberate-but-deferred cleanups. Not bugs (those get fixed now) — these are
naming, structure, or ergonomics improvements that are safe to defer. Each entry: what,
why it's debt, where, and the rough fix. Remove an entry when it lands.

| # | Item | Why it's debt | Location | Fix |
|---|------|---------------|----------|-----|
| 1 | Agentic tool-use falls to `claude_code` under load (no non-NIM tool fallback) | **ROOT CAUSE VERIFIED 2026-06-28.** Not a provider limitation: direct probes show **every** provider does tool-calls fine — `mistral-large-3`, `qwen3.5`, `deepseek`-direct, `groq` llama, `kimi` all return `tool_calls`. The real cause (SP-MULTIDEMO dry-run, `server.log`): the agentic loop's burst of large requests (32k tokens, 13 msgs, 7 tools) makes NIM **rate-limit (HTTP 429)** — **28×** in the run → **11×** `proxy_chain_failover_fired` (= the 11 agentic turns that landed on `claude_code`). And the coder chain `[mistral-large(NIM), deepseek-v4-pro(NIM), claude_code]` has **no non-NIM tool-capable fallback** before the `claude_code` tail, so a NIM 429 falls straight to Max. Secondary: the proxy adds `chat_template_kwargs` (thinking) for Mistral → NIM 400 "chat_template not supported for Mistral tokenizers" — REAL but already retry-handled (`nvidia_nim/client.py` strips it), just a wasted round-trip. | `server.log` (429 / `proxy_chain_failover_fired`); coder chain in `chains.env`; `request.py:132-138` (chat_template for Mistral) | Give the chains a **tool-capable non-NIM fallback** (deepseek-direct / groq / kimi — all verified) **before** the `claude_code` tail, so a NIM 429 fails over to another real provider, not Max. Pre-emptively skip `chat_template_kwargs` for Mistral models. NB: the `CODER` tier is being removed (coding → `sonnet`), so this applies to the SONNET chain. |
| 2 | `MAX_CODER_ROUNDS` fixed cap contradicts "iterate until the work is done" | The findings-driven Coder loop (`review fix → push → CI → re-review → fix`) is bounded by `MAX_CODER_ROUNDS = 3` (`stuck.py:47`). The agentic author→test loop already iterates to green per step; the OUTER review-fix loop should likewise run **while it makes progress** and only stop/escalate when it **stalls** — not at an arbitrary round count. `assess_stuck` already detects no-progress, so the fixed cap is redundant with (and weaker than) the real signal. Operator call 2026-06-28: a round-count cap is the wrong mechanism. | `src/ferova/review/stuck.py:47` (`MAX_CODER_ROUNDS`), `assess_stuck` (`stuck.py:176`), surfaced in `cli/review_cmds.py:189` | Drop the fixed round-count cap; rely on the existing **no-progress / stall** detection to terminate + escalate (keeps the non-convergence safety without an arbitrary ceiling). Re-point any test asserting the count cap to the stall condition. |
| 3 | Sentinel token-budget inversion (**bug-class — first in line, not a deferral**) | The base `Reviewer.max_tokens` default is **4096**, sized after live mid-JSON truncation at the historic 1500 cap (PR #93: reasoning models emit CoT before the structured JSON — rationale at `reviewer.py:391-405`). Sentinel still overrides `max_tokens = 2000` with a docstring claiming it is "bumped to 2000 so security findings have room for detail" — a stale scar from the 1500 era. Net effect: the security reviewer, the only bot on `PROXY_OPUS_CHAIN` (where reasoning models are most likely), now has the LOWEST output budget of the bench, re-exposing exactly the truncation class the 4096 bump fixed. (verified 2026-07-02) | `src/ferova/review/reviewer.py:869-880` (override + stale docstring) vs `:391-405` (base default + incident rationale) | Delete the Sentinel `max_tokens = 2000` override (inherit 4096) and the "bumped to 2000" docstring sentence; add a unit test pinning Sentinel's budget ≥ the base default. |
| 4 | Stale "NIM" prose and `NimAgentOutput` naming survive the reviewer-chain rename | SP-REVIEWER-CHAIN-RENAME renamed the constant, but the surrounding vocabulary still claims a NIM-only pipeline: `review/__init__.py:3` ("Five specialised bots, all running on NVIDIA NIM"), `review/spec.py:8/14/21` ("Reviewer NIM team", "every NIM bot", "a NIM prompt"), `prompts/review/developer_0.2.0.md:1/10/15` ("NIM-only review-bot pipeline"), the `NimAgentOutput` class itself (`agent_engine/agent_loop.py:133`, exported at `agent_engine/__init__.py:14/21`, cited in four `reviewer.py` docstrings), and `review/thread_context.py:25` cites `<role>_0.3.0.md` while reviewers are at 0.4/0.5. Every agent actually walks the full failover chain via the proxy. (verified 2026-07-02) | listed per file in the previous column | One truthful-naming sweep: rename `NimAgentOutput` (e.g. `AgentOutput`) with its docstring references, rewrite the three module docstrings, re-version `developer_0.2.0.md` with corrected prose, fix the `thread_context.py` version citation. |
| 5 | `orchestrator.py` module docstring describes the retired verdict-first pipeline | The header says the orchestrator "Aggregates their verdicts into a final team verdict" and "does **not** auto-merge anything and does **not** auto-invoke the Coder bot — those are explicit follow-up actions (future :func:`run_coder_response`)". All stale since the evidence-first flip: the team verdict is derived from the findings ledger (`merge_gate.verdict_from_facts`), and auto-merge + the Coder fix loop exist and run from `auto-review.yml`. (verified 2026-07-02) | `src/ferova/review/orchestrator.py:1-19` | Rewrite the module docstring to describe the current findings-ledger pipeline (reviewers → findings → verification → derived verdict → report-only archive). |
| 6 | Reviewer prompts ground their examples in a prior project's artifacts | The persona prompts cite files that do not exist in this repo: `architect_0.5.0.md:20` (`bot/whatsapp_client.py`), `architect_0.5.0.md:51` (`src/ferova/agents/match_analyst.py`), `scribe_0.5.0.md:10-11` ("bilingual WhatsApp layer (`agents/wa_chat.py`, `prompts/wa_chat/*`)"), `tester_0.4.0.md:17` ("Tsitsipas, S." tennis names). Misleading few-shot grounding for the bench; the off-diff comment filter is what currently absorbs any reviewer citing these phantom paths. (verified 2026-07-02) | `prompts/review/architect_0.5.0.md:20,51`, `prompts/review/scribe_0.5.0.md:10-11`, `prompts/review/tester_0.4.0.md:17` | Re-version the three prompts with Ferova-native examples. Operator-only work: `prompts/review/*` is on the bot path whitelist's forbidden list, so no bot can fix this. |
| 7 | Docs lag shipped code on four surfaces | (a) `docs/model_first_chains_architecture.md:3` still reads "design approved, implementation not started (2026-06-30)" though all five SP-MFC slices + `ferova regenerate-chains` are live; (b) `docs/proxy_routing_redesign_architecture.md:171-177` checkmarks only slices A/B though C0/C1/C/D/E all shipped; (c) `docs/chain_autopilot_architecture.md:142-159` checkmarks only 0b/0b-2 though phases 1-3e exist in code (`review/decision.py`, `audit_log.py`, `chain_apply.py`, the systemd cadence); (d) `CLAUDE.md:72` layout tree says `tests/unit` is "the suite CI requires green" though `ci.yml:61-62` also gates on `tests/integration`. Also: `deploy/systemd/README.md:3-5` references a `ferova-nim-health` timer whose units are not version-controlled, and three diagram sets (`builder_memory_architecture`, `nim_observability_architecture`, `review_factory_architecture` .png/.svg) are referenced by no doc. (verified 2026-07-02) | listed per file in the previous column | One docs-refresh pass: status lines and checkmarks with PR numbers, CLAUDE.md layout row, commit the nim-health units (or drop the mention), link or delete the orphaned diagrams. |
| 8 | `chains.env` header commentary contradicts its machine-regenerated values | Header rules #5/#6 ("every chain LEADS with a NIM entry", "both tiers now LEAD with a warm Mistral") and the per-tier notes (`chains.env:40-55,67`) describe an ordering the generated values no longer follow: `MODEL_OPUS=open_router/z-ai/glm-5.2,claude_code/opus` (line 71) has no NIM and no Mistral, and no `MODEL_*` value contains a Mistral model at all. `chain_generate.py` preserves non-`MODEL_*` lines verbatim, so the prose keeps rotting on every 6h regeneration. (verified 2026-07-02) | `chains.env:40-55,67` (stale rules) vs `:71,78` (actual values); `src/ferova/llm_proxy/routing/chain_generate.py` | Make `chain_generate.py` render the header/per-tier commentary from the same data it renders the chains from, or strip the hand-written rationale down to a pointer at the generator. |
| 9 | MCP tool whitelist is inert plumbing | `allowed_tools_for(role)` is computed and stored as `self._allowed_tools` on every bot, but the only readers are test assertions (`tests/unit/test_review_mcp_whitelist.py:133/143/153`); `agent_engine/` contains zero occurrences of `allowed_tools`, so AgentLoop never receives or enforces the whitelist — reviewers are tool-less one-shots by design. SP-MCP-EXT-B (the wiring) never landed. (verified 2026-07-02) | `src/ferova/review/reviewer.py:436,960,1545` | Wire `_allowed_tools` into AgentLoop tool registration when MCP tools actually flow through the loop, or delete the per-bot plumbing and its tests until then. |
| 10 | `findings_bridge._files_in_diff` docstring cites a counterpart that does not exist | The docstring says it "mirrors ``coder_loop._files_in_diff`` deliberately; the duplicate is temporary until the legacy arbiter is retired with that module" — but grep finds only this one definition; `coder_loop.py` has no diff-header parser and never did in the repo's visible history. Stale cross-reference, not a live duplicate. (verified 2026-07-02) | `src/ferova/review/findings_bridge.py:53-55` (docstring), `:45` (sole def) | Rewrite the docstring to drop the `coder_loop` cross-reference, keeping the failure-soft rationale. |
| 11 | `review/chain_health_store.py` back-compat shim still has live importers | The module is a pure re-export over `ferova.health.store` (SP-HEALTH-STORE-NEUTRALIZE), but `cli/main.py:98` and `tests/unit/test_chain_health_store.py:19` still import through the old path, so the shim cannot be deleted yet. (verified 2026-07-02) | `src/ferova/review/chain_health_store.py:1-23`, `src/ferova/cli/main.py:98`, `tests/unit/test_chain_health_store.py:19` | Migrate the two importers to `ferova.health.store`, then delete the shim and its identity test (`test_health_store_neutral.py:50`). |
| 12 | Dead config and dependencies | (a) `prompts/review/developer_0.1.0.md` is referenced by nothing (code pins 0.2.0 at `reviewer.py:1505`); (b) `hypothesis>=6.108` (`pyproject.toml:35`) is a dev dep with zero property-based tests; (c) both lint scanners list a phantom `"agents"` root (`no_inline_comments.py:43`, `no_silent_except.py:45`) that does not exist and is silently skipped; (d) `edge_honesty.load_frontier_suppress` (`edge_honesty.py:171-190`) reads `[tool.ferova.arch] frontier_suppress` from `pyproject.toml`, which has no `[tool.ferova]` section at all — the loader always returns empty. (verified 2026-07-02) | listed per item in the previous column | Delete the dead prompt; drop hypothesis or write the first property-based test; remove the phantom root from both scanners; add the `[tool.ferova.arch]` table or remove the dead loader. |
| 13 | Lint-gate collision on the `# allow-silent-except:` escape hatch | `no_silent_except` honours the directive only on the `except` statement's own source line (`no_silent_except.py:256-258`), but `no_inline_comments` flags any comment on a line that also carries code tokens (`no_inline_comments.py:123-124`) with no exemption — an `except` always tokenises as code, so the first real use of the escape hatch fails the SP-NO-INLINE-COMMENTS-GATE. Latent today only because `MAX_SILENT_EXCEPT = 0` and zero suppressions exist. (verified 2026-07-02) | `src/ferova/lint/no_silent_except.py:256-258` vs `src/ferova/lint/no_inline_comments.py:123-124` | Resolve before first use: exempt the directive in `no_inline_comments.scan_file`, or teach `_allow_suppressed` to accept a standalone comment line above the `except`. |
| 14 | Required CI check names hand-synced with ci.yml | `DEFAULT_REQUIRED_CHECK_NAMES = ("Test suite (Python 3.11)", "Test suite (Python 3.13)")` mirrors the ci.yml matrix by hand — the module docstring admits it ("mirrors the matrix in ``.github/workflows/ci.yml``. Override per call when the matrix changes", `auto_merge.py:33-35`). A matrix bump silently desynchronises the auto-merge CI gate, which fails closed on the now-missing check names. (verified 2026-07-02) | `src/ferova/review/auto_merge.py:87-90`; `.github/workflows/ci.yml:16` | Add a unit test asserting the tuple matches ci.yml's parsed matrix (cheap), or derive the names from a shared source. |
| 15 | ruff scope drift across the three lint surfaces | `CLAUDE.md:145` documents `ruff check src tests scripts`, but `ci.yml:41,44` and `ci_local.sh:115` run only `src tests`; `scripts/*.py` gets ruff solely via pre-commit on staged files — a script pushed with `--no-verify` or edited outside a commit lands unlinted in CI. (verified 2026-07-02) | `CLAUDE.md:145` vs `.github/workflows/ci.yml:41` and `scripts/ci_local.sh:115` | Add `scripts` to the ruff invocations in ci.yml and ci_local.sh (preferred), or fix the CLAUDE.md command to match reality. |
| 16 | `arch check` placement and repo-root derivation | The command is registered in `cli/main.py` (not `arch/cli.py` beside its sibling `arch graph`), and derives `repo_root = Path(__file__).resolve().parents[3]` — correct only for the src-layout editable install; a wheel install resolves into the interpreter's site-packages tree, not any repo. (verified 2026-07-02) | `src/ferova/cli/main.py:27-36` | Move the command next to `arch graph` and resolve the root via `git rev-parse --show-toplevel` (falling back to cwd). |
| 17 | `ferova develop` exit codes substring-match free-text `no_op_reason` | `review_cmds.py:486-498` maps exit codes 3-7 via `"spec not found" in reason`, `"no fixes" in reason`, `"self-verify" in reason`, etc. (second copy at `:559` in `review plan`) — a harmless rewording in `dev_runner.py` silently changes CLI exit semantics that `auto-review.yml` and operator scripts key off. (verified 2026-07-02) | `src/ferova/cli/review_cmds.py:486-498,559` | Make `no_op_reason` a typed enum (or add an explicit exit-code field to `DeveloperSessionResult`) and map exit codes from it. |
| 18 | ChainPilot arc lives inside `review/` | Ten modules tagged SP-CHAINPILOT-* (`audit_log`, `chain_apply`, `chain_loop`, `chain_placement`, `chain_plan`, `chain_rewrite`, `coder_outcomes`, `decision`, `perf_aggregate`, `reviewer_outcomes`) sit in `src/ferova/review/` though they form a distinct subsystem (autonomous chain mutation) that merely *reads* the review tables as its signal; CLAUDE.md's layout section does not mention it. (verified 2026-07-02) | `src/ferova/review/chain_loop.py` and the nine siblings above | Extract to `src/ferova/chainpilot/` with back-compat re-export shims, mirroring the SP-HEALTH-STORE-NEUTRALIZE pattern. |
| 19 | Developer step audits overload `pr_coder_responses` with `pr_number=0` | `execute_plan_step` records every Developer step dispatch via `record_coder_response(db, pr_number=0, ...)` (`dev_runner.py:660-671` → `persistence.py:444`) — sentinel rows in a PR-keyed table; every per-PR consumer (including ChainPilot's outcome harvesting) must know to exclude `pr_number=0`. (verified 2026-07-02) | `src/ferova/review/dev_runner.py:660-671`; `src/ferova/review/persistence.py:444` | Add a dedicated dev-session audit table, or a nullable session/spec_id column, instead of the sentinel PR number. |
| 20 | Default `FEROVA_DB_PATH` resolves inside the repo working tree | `db_path` defaults to `./data/ferova.db` (CWD-relative, `config.py:71-72`); the live factory ledger sits gitignored (`.gitignore:37`) inside the same tree where bots write code and gates run. CI re-points it per job (`auto-review.yml` sets it into runner temp), but local runs mix state and workspace. (verified 2026-07-02) | `src/ferova/core/config.py:71-72`; `data/ferova.db` | Default to an XDG data dir (e.g. `~/.local/share/ferova/ferova.db`), or document that operators must set `FEROVA_DB_PATH` outside the tree. |
| 21 | free-claude-code lineage vestiges in `llm_proxy` | Vendored-origin leftovers: `~/.config/free-claude-code/.env` in the settings env-file order + `FCC_ENV_FILE` hook (`config/settings.py:35,38`); `PTB_TIMEDELTA` (python-telegram-bot) env set at import (`api/app.py:20-21`); FastAPI title "Claude Code Proxy" (`api/app.py:51`, echoed at `api/runtime.py:56`); `/v1/models` advertises stale-or-fictional Anthropic ids unrelated to what the proxy serves (`api/routes.py:19-55,159-167` — `claude-haiku-4-20250514` never existed). `licenses/free-claude-code-LICENSE` stays (attribution). (verified 2026-07-02) | listed per file in the previous column | Strip the FCC env path/hook and `PTB_TIMEDELTA`, rename the app title, and generate `/v1/models` from the configured chains; keep the LICENSE file. |
| 22 | Breaker + effort-map restart amnesia | Both are process-level singletons (`routing/breaker.py:85-90`, `providers/effort_map.py:69-74`) seeded only from persisted probe history at startup (`api/runtime.py:60-61`); trips and effort resolutions learned during a run are lost on restart, and two proxy processes share nothing. (verified 2026-07-02) | `src/ferova/llm_proxy/routing/breaker.py:85-90`; `src/ferova/llm_proxy/providers/effort_map.py:69-74` | Persist trips/resolved efforts to the existing SQLite probe tables on mutation, or document restart amnesia as accepted design. |
| 23 | Missing ledger artifact stalls merges with a misleading reason | When the findings-ledger artifact fails to download (`auto-review.yml:672-676` is `continue-on-error`), `auto_merge`/`evaluate_merge_gate` create a fresh empty schema (`auto_merge.py:431-432,502-503`) and the gate refuses with "no review-integrity record at head (review did not run)" (`merge_gate.py:115-116`) — correctly fail-closed, but the reason misdiagnoses an artifact-transport failure as a missing review, sending the operator to the wrong place. (verified 2026-07-02) | `src/ferova/review/merge_gate.py:115-116`; `src/ferova/review/auto_merge.py:431-432,502-503`; `.github/workflows/auto-review.yml:672-676` | Detect the absent/empty-ledger case explicitly and emit a distinct reason (e.g. "findings ledger missing at head — artifact transport failed?"). |
| 24 | Developer contract-escape check counts pre-existing untracked files as step escapes | The changed-paths capture reads `git status --porcelain --untracked-files=all` (`dev_runner.py:556-568`), so any untracked file already sitting in the worktree BEFORE the session (an uncommitted spec draft, a stray lockfile) is attributed to the step and trips the "changed files outside its contract" refusal. Observed live 2026-07-02 on SP-FINDINGS-BRIDGE-DOCFIX attempt 1: the step was refused for the spec's own uncommitted file plus `uv.lock`, neither written by the Developer. | `src/ferova/review/dev_runner.py:556-568` (capture); contract-escape refusal in `execute_plan_step` | Snapshot untracked paths at session start and subtract them from the step's changed-paths set — or refuse to START a session in an untracked-polluted tree with a clear message, mirroring safe_merge's refuse-dirty-tree rule. |
| 25 | Import gate false-positives on valid submodule from-imports (**bug-class — blocks legitimate Developer steps**) | `check_imports` resolves `from ferova.review import findings_bridge` to package `ferova.review`, then checks the alias only against `_top_level_names(__init__.py)` — never against the package's submodule files — so the perfectly valid submodule import is reported as "'findings_bridge' is not defined in 'ferova.review'; found in: (nowhere under ferova)" (`_find_name_homes` matches defined names, not module basenames). Observed live 2026-07-02 on SP-FINDINGS-BRIDGE-DOCFIX attempt 2: the gate refused the step twice and the session gave up, though the test imported an existing module. Any `from <package> import <submodule>` in Developer output hits this. | `src/ferova/review/import_gate.py:154-175` (ImportFrom branch), `:53` (`_top_level_names`), `:77` (`_find_name_homes`) | In the ImportFrom branch, before consulting `_top_level_names`, accept `alias.name` when `<src_root>/<dotted-as-path>/<alias.name>.py` (or `<alias.name>/__init__.py`) exists; extend `_find_name_homes` to also report submodule homes. |
| 26 | Local review can run against a stale PR head right after a push | `ferova review pr <N>` launched in the same shell pipeline as `git push` reviewed the pre-push head: on PR #3 (2026-07-02) the re-review recorded integrity + findings at `e9c0905` while the freshly pushed GitHub head was `11a1d9a` — `gh pr diff`/`gh pr view` served the not-yet-propagated head. The evidence-first gate caught it (review_complete false at the true head), but the run's cost was wasted and the operator sees a confusing "complete" review at the wrong SHA. | `src/ferova/review/orchestrator.py` (head resolution at review start) | At review start, compare the fetched `headRefOid` with the locally checked-out HEAD when they should match (operator runs from the branch), or at minimum log the reviewed SHA prominently and retry the fetch when it differs from an explicitly passed `--expect-head`. |
